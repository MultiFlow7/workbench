---
version: v0.6.0
codename: Control Room
date: 2026-05-19
status: released
---

# v0.6.0 · Control Room

## 概述

v0.6 完成了工作台 Agent 管理 UI 层的全面建设，将后端状态机（v0.5 已有）首次与可操作的前端界面打通。用户可以通过控制台模式和工具管理模式完整管理 Agent 任务生命周期。

同期部署了 v0.7 Dispatch Layer（真实调度）代码，使任务触发后 5 秒内自动被 dispatcher 接取并调用 Claude API 执行。

---

## 已交付需求

| req | 标题 | 验收结论 |
|-----|------|---------|
| req-013 | Agent 任务状态机（后端） | ✅ done — 6 态状态机，终态保护（422），CRUD 完整 |
| req-016 | 多层级任务可视化（精简版） | ✅ done — TaskOverview 按状态分 Tab，SSE 实时更新 |
| req-018 | 决策收件箱（非阻塞人工决策队列） | ✅ done — DecisionInbox 按风险等级排序，Approve/Reject/Snooze |
| req-030 | Agent 注册表 UI | ✅ done — AgentList（16 个 Agent）+ AgentDetail（doc 降级处理） |
| req-031 | Agent 任务手动触发 UI | ✅ done — TaskTriggerForm 含 file_refs / trigger_reason 字段 |

## 遗留（随 v0.6 继续进行）

| req | 状态 | 说明 |
|-----|------|------|
| req-017 | in-progress | 执行流视图点击任务卡片展开功能未实现，SSE 基础已就绪，detail view 推 v0.7 |

---

## 后端（Axum，:8081）

### 新增 API

| 路由 | 方法 | 说明 |
|------|------|------|
| `/api/tasks` | POST | 创建任务（含 file_refs / trigger_reason v0.7 字段） |
| `/api/tasks` | GET | 列表查询（status / role / project 过滤） |
| `/api/tasks/:id` | GET | 单任务详情 |
| `/api/tasks/:id/status` | PUT | 状态迁移（终态保护 → 422） |
| `/api/tasks/stats` | GET | 按状态分组统计 |
| `/api/decisions` | GET | 决策队列列表 |
| `/api/decisions/:id/resolve` | POST | 解决决策 |
| `/api/events/stream` | GET | SSE 任务事件流 |
| `/agents/registry` | GET | Agent 注册表（读 registry.yaml） |
| `/agents/:role/doc` | GET | Agent 文档（AGENT.md，降级到 agent-roster.md） |
| `/sse/notifications` | GET | SSE 通知流（v0.7 新增） |

### 关键修复

- **终态保护**：`update_task_status` 校验 Completed/Failed 为不可逆终态，尝试迁移返回 HTTP 422
- **list_tasks 签名修正**：dispatcher 内部调用由旧 4 参数形式统一为 `TaskFilter` struct
- **新增 `AppError::InvalidInput`**：HTTP 422 UNPROCESSABLE_ENTITY，用于输入校验失败

### v0.7 同期部署（不计入 v0.6 验收范围）

- `DispatcherConfig` + `run_auto_dispatcher()` tokio background task（Semaphore(4)，600s timeout）
- `ContextBuilder`：4 层上下文注入（role prompt / task state / file_refs / trigger_reason）
- `SseNotification` enum + `/sse/notifications` SSE 通知流
- `check_r001_rule()`：30s 轮询 technical.md 100% → 自动创建 qa-agent 任务
- `ui_events` 全埋点（6 种事件类型）

---

## 前端（workbench/src，Vite + React）

### 新增组件

| 组件 | 路径 | 说明 |
|------|------|------|
| TaskOverview | `components/TaskOverview/` | 状态分 Tab 任务列表，SSE 驱动刷新 |
| TaskCard | `components/TaskOverview/TaskCard.tsx` | 任务卡片（角色/标题/项目/耗时/状态标签） |
| DecisionInbox | `components/DecisionInbox/` | 决策列表，按风险等级排序，Approve/Reject/Snooze |
| DecisionCard | `components/DecisionInbox/DecisionCard.tsx` | 单条决策卡片 |
| TaskTriggerForm | `components/TaskTrigger/` | 触发表单（角色/类型/描述/文件引用） |
| AgentList | `components/AgentRegistry/AgentList.tsx` | 直接 HTTP fetch registry，dot 状态指示器 |
| AgentDetail | `components/AgentRegistry/AgentDetail.tsx` | Agent 文档 + 活跃任务（HTTP fetch） |

### App.tsx 路由变更

- 控制台模式 P3：`ConsoleTabView`（任务总览 / 决策收件箱 Tab）
- 控制台模式 P4：TaskTriggerForm（点「+ 触发任务」展开）
- 工具管理模式 P2：AgentList
- 工具管理模式 P4：AgentDetail

### TypeScript 修复

- `AgentList.tsx`：`res.json()` 泛型从 `Promise<AgentInfo[]>` 改为 `Promise<Array<{id, path, status, description}>>`，与后端实际 schema 一致

### v0.7 前端（同期部署）

- `useNotifications` hook（EventSource，连接 `/sse/notifications`）
- `notificationsSlice` + TopBar 瞬态通知提示
- `TaskTriggerForm` 新增 `file_refs` 文本域

---

## 服务器部署

- 地址：`43.135.174.27:8081`
- 构建：`cargo build --release`（增量编译，~20s，0 errors）
- 服务：`systemd workbench-backend.service`
- 新增：`/data/agent-registry/registry.yaml`（16 个 Agent）
- 新增环境变量：`WORKSPACE_ROOT=/data/workbench`

---

## QA 验收结果

### 后端 API（全部通过）

| 用例 | 结论 |
|------|------|
| T1-1 创建任务 POST /api/tasks | ✅ task_id 返回正确 |
| T1-2 自动调度：Pending → Running → Completed | ✅ 5s 内接取，15s 内完成，输出为真实 Claude 响应 |
| T1-3 终态保护：Completed → Pending | ✅ 返回 HTTP 422 |
| T1-4 决策列表 GET /api/decisions | ✅ 返回 [] 无崩溃 |
| T1-5 解决决策 | ⏸ 需 CEO-event 触发，前端集成测试 |
| T1-6 SSE 端点 | ✅ /api/events/stream + /sse/notifications 均 200 + SSE 头 |
| T-Reg Agent 注册表 | ✅ 返回 16 个 Agent |
| T-Stat 任务统计 | ✅ 按状态分组数据正确 |

### 前端组件（代码验证）

| 组件 | 验证方式 | 结论 |
|------|---------|------|
| AgentList | HTTP 验证（16 agents） | ✅ |
| AgentDetail doc | HTTP 验证（AGENT.md 降级处理） | ✅ 降级到「无法加载」提示 |
| TaskOverview | TypeScript 编译 + 代码审查 | ✅ 逻辑正确 |
| DecisionInbox | TypeScript 编译 + 代码审查 | ✅ 逻辑正确 |
| TaskTriggerForm | TypeScript 编译 + 代码审查 | ✅ 逻辑正确 |
| TypeScript 全量检查 | `tsc --noEmit` | ✅ 0 errors |

---

## 已知遗留

1. **req-017 执行流 detail view**：点击任务卡片后的执行步骤流视图未实现（SSE + ui_events 基础已就绪），推 v0.7
2. **SseNotification::DecisionRequested**：变体已定义，通知路径走 create_decision()（pending count SSE），未走 notify_tx，v0.8 一并完善
3. **AGENT.md 文件未部署**：server `/data/agent-registry/agents/*/AGENT.md` 不存在，AgentDetail doc 面板显示降级提示
4. **前端 Tauri invoke 验收**：TaskOverview / DecisionInbox / TaskTriggerForm 依赖 Tauri 运行时，需在 Tauri app 中完整验收（T2-T6）

---

*由 workbench-ceo 于 2026-05-19 生成*
