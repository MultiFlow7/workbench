# v0.15 节点 1.1
# 关闭 pnpm 9+ 的 dep status pre-flight 检查
# （electron postinstall 已手工跑过，二进制就位；
#  pnpm.onlyBuiltDependencies 还未走 approve-builds 流程，
#  pre-flight 会阻塞 dev 启动）
verify-deps-before-run=false
