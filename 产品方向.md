---
project: 工作台
created: 2026-05-15
updated: 2026-05-17
tags:
  - 类型/规划文档
  - 主题/技术/工作台
  - 状态/进行中
---

# 产品方向 · 工作台

---

## 长期愿景

**工作台是我与 AI 的协作平面**——不只是「AI 为我工作」，而是三种关系并存：
- 我服务于 AI（提供需求、决策、上下文）
- AI 服务于我（执行任务、生成内容、分析问题）
- 我管理 AI（调度 Agent 团队、监控执行状态、设定规则）

这是一体两面的产品逻辑：**后端逻辑 AI first**（Agent 调度、状态机、上下文构建、并发编排），**前端逻辑 Human first**（可视化、决策收件箱、任务管理、执行透明化）。两者不冲突，共同服务于「降低人管理 AI 团队的认知负担，同时保留人的控制权」这一核心目标。

界面交互是第一个落地点（当前阶段），通过做界面来明确需求；AI 团队管理是中期演化方向（v0.3+）。

---

## 第一个落地的定义

> 不追求完整，追求「用起来比三个独立窗口更顺」。

**核心场景**：同时看着对话历史和画布节点树，能在不同分支间切换视角，找到 bug 来源，在 QA sweep 结果出来时当场在工具面板里读到细节——**这件事发生**，就算落地了。

**四个核心动作**：

| 动作 | 描述 | 衡量标准 |
|------|------|---------|
| **切** | 不同工作模式（对话/工具/控制台）无缝切换 | 点击导航图标，<200ms 完成，状态保留 |
| **看** | 结构（Panel 2）和内容（Panel 3）同时在视野内 | 不需要切窗口，一屏看清分支结构和对话内容 |
| **选** | 在对话树里选择父节点，Panel 3 自动呈现对应线性历史 | 点击节点→对话视角切换，无刷新 |
| **跨** | 内容可以在工作区之间转移 | 节点/消息可拖拽到另一个工作区标签页 |

---

## 战略意图

- **四面板布局（参考 Obsidian）**：最左侧导航栏 + 列表 / 结构面板 / 主工作区 / 右侧详情面板，每个面板职责明确，可独立折叠
- **多工作区**：类似 macOS 多桌面，每个工作区标签页记住自己的模式和焦点状态；可以从一个工作区把内容拖到另一个
- **对话从卡片改为线性 chatbot 模式**：分叉树在 Panel 2 可视化，Panel 3 始终是连贯的线性对话视图；切换哪个节点对话，Panel 3 自动呈现从根到该节点的完整历史
- **将来一定要能做到**：控制平面的工具管理界面和对话界面可以侧边并排，在对话时随手切换工具，不跳窗口
- **将来一定要能做到**：跨工作区拖拽，比如把 A 画布的节点直接拖进 B 工作区
- **不想堵死**：Panel 的个数和布局方向未来可配置（竖排 / 自定义）
- **不想堵死**：工作区标签页内的内容类型未来可扩展（当前：对话/工具/控制台，将来：文档/白板/代码）

---

## 架构原则

- 因为「四面板职责明确」，Panel 之间只通过「选中状态事件」通信，不直接调用彼此的渲染逻辑
- 因为「对话线性化」，对话历史的路径计算（根→当前节点的 path）在状态层完成，Panel 3 只负责渲染给定的消息列表
- 因为「跨工作区拖拽」，每个 Panel 的内容单元（节点、消息、技能）都需要序列化为可拖拽的 payload，不能有只存在于 DOM 的隐式状态
- 因为「将来可配置布局」，Panel 的宽度、可见性以配置对象表示，不硬编码在 CSS 中

---

## 各面板职责边界

| 面板 | 职责 | 不做的事 |
|------|------|---------|
| **Panel 1（导航）** | 模式切换 + 列表浏览（项目/工具/服务） | 不渲染结构，不渲染内容 |
| **Panel 2（结构）** | 可视化当前模式的结构（分支树/技能注册表/服务列表） | 不处理业务逻辑，不渲染详情 |
| **Panel 3（主工作区）** | 当前焦点内容（线性对话/技能内容/执行面板） | 不管理结构，不显示上下文详情 |
| **Panel 4（详情/编辑区）** | 上下文详情 + 文档查看/编辑（渐进演化为第二工作区） | 不触发主业务操作（P3 发起，P4 响应） |

---

## 当前阶段进度

1. ✅ 需求草稿整理（2026-05-15）
2. ✅ HTML 原型（prototype.html，2026-05-15）
3. ⬜ 用户认可原型后：拆解具体产品需求（req-001 起）
4. ⬜ 技术规划：选技术栈，设计 Panel 状态管理方案
5. ⬜ v0.1 实现：4 面板布局 + 对话模式（线性化 + 分支树）
6. ⬜ v0.2 实现：工具管理模式（SKILL.md 查看器 + 注册表）
7. ⬜ v0.3 实现：控制台模式 + 多工作区
8. ⬜ 跨工作区拖拽（需要完成 v0.1~v0.3 后再定方案）

---

## 架构决策（已确认）

| 决策 | 内容 | 确认日期 |
|------|------|---------|
| **产品形态** | Tauri 桌面端应用（非 web），后端跑在服务器，部分逻辑调用本地，前端为 Tauri 封装的 React | 2026-05-17 |
| **P4 演化方向** | P4 渐进成为「第二工作区」：只读详情 → 文档查看器 → 可编辑器 → 支持打开 Obsidian vault 中任意文件 | 2026-05-17 |
| **多工作区 Tab** | 顶部 Tab 是真正独立的工作区（各自记住模式+焦点状态），不是模式预设快捷键；用户会同时开多个会话 | 2026-05-17 |
| **AI 工具层架构** | 建立独立 Python 后端服务（Model Router + LLM Gateway），部署在用户自己的服务器上；Tauri 前端不再直接调 AI API，改为调此服务；Auth 暂不实现，预留多用户协作扩展点 | 2026-05-23 |
| **多端同步策略** | 近期目标：多端互通但不经自建账号服务器同步，由用户自行接入 iCloud/Dropbox/Git 等云服务（类 Obsidian 模式），产品不介入同步机制 | 2026-05-23 |

---

## 更新记录

| 日期         | 变化                                               |
| ---------- | ------------------------------------------------ |
| 2026-05-15 | 初稿，基于需求草稿和三项目现状整理                                |
| 2026-05-17 | 确认三项架构决策（Tauri 桌面端、P4 演化路径、真实多工作区 Tab）；P4 职责边界更新 |
| 2026-05-18 | 长期愿景升级为「协作平面」定位：一体两面（后端 AI first / 前端 Human first）；确认当前阶段以交互界面为起点、AI 团队管理为中期演化方向；新增 req-013~021 进入 backlog |
| 2026-05-23 | 确认 AI 工具层架构决策：独立 Python 后端服务（Model Router + LLM Gateway），部署在用户服务器；确认多端同步采用类 Obsidian 模式，不自建账号同步服务器（req-047） |
