---
project: 工作台
created: 2026-05-15
tags:
  - 类型/规划文档
  - 主题/技术/工作台
  - 状态/进行中
---

# 原型设计意图 · prototype.html

> 记录 HTML 原型的设计决策，以便在用户确认方向后，这里的判断可以直接转化为产品需求和技术约束。

---

## 原型覆盖范围

prototype.html 演示了三个核心模式 × 四个面板的交互框架：

| 模式 | Panel 2 | Panel 3 | Panel 4 |
|------|---------|---------|---------|
| 对话模式 | 分支树（SVG，可点击节点） | 线性 chatbot，含分支面包屑 | 节点详情 + 最后一轮 QA |
| 工具管理 | 技能注册表（卡片列表） | SKILL.md 胶水逻辑 + 调用顺序 | 技能元数据 |
| 控制台 | 服务状态列表 | 执行终端（可输入命令） | 模型用量分布 |

---

## 关键设计决策

### 1. 对话分叉的可视化方案

**问题**：现有无限画布是卡片+画布，浏览上下文不流畅。新方案要线性对话，但保留分叉能力。

**决策**：Panel 2 放分支树（节点图），Panel 3 始终是线性 chatbot。点击 Panel 2 中的节点，Panel 3 自动呈现从根到该节点的完整路径消息，顶部面包屑显示当前路径。

**取舍**：
- 放弃：在 Panel 3 中直接展示分叉选择器（会破坏线性视角）
- 放弃：在 Panel 2 中渲染完整对话内容（会信息过密）
- 选择：Panel 2 纯结构，Panel 3 纯内容，通过点击事件联动

**需验证**：用户能否从 Panel 2 的节点标签快速判断「哪个节点是我想继续的那个」——如果不能，需要在树节点上加更多信息（摘要/时间/问题片段）。

---

### 2. 工作区标签页 vs 模式切换

**问题**：应该用顶部 Tab 切换（类浏览器）还是左侧导航图标切换（类 IDE）？

**决策**：两层结合——顶部 Tab 是「工作区」（记住独立的焦点状态），左侧导航是「模式」（对话/工具/控制台）。顶部 Tab 内的模式可以不同。

**原型实现**：简化为「顶部 Tab = 模式预设」（Tab 0 = 对话模式，Tab 1 = 工具模式，Tab 2 = 控制台），实际产品中 Tab 应该可以独立配置模式。

**需验证**：用户是否会同时开多个对话模式的 Tab（比如「无限画布开发」和「工作台规划」同时可见）？如果是，Panel 2 的分支树要支持切换项目。

---

### 3. Panel 4 的定位

**问题**：右侧面板放什么？

**决策**：放「点击结果的上下文详情」——不是主操作区，而是辅助阅读区。点击 Panel 2 的节点或 Panel 3 的某条消息，Panel 4 显示相关元数据和内容摘要。

**需验证**：用户是否会把 Panel 4 当主要工作区（比如编辑技能内容）？如果是，Panel 4 需要支持变成可编辑面板，而不只是只读详情。

---

### 4. 设计风格（源自 canvas-design-brief SKILL）

- **场景判定**：工具型（开发者工作台，功能密度高）→ Minimalist 变体
- **字体**：Inter（结构型标签和 UI 文字）/ JetBrains Mono（代码和终端）
- **动效**：CSS（功能型过渡，如面板折叠）；Spring 仅用于展示型元素（暂未实现）
- **颜色**：浅色主题，#f5f5f5 背景，白色 surface，蓝色 accent（#2563eb）
- **不做**：深色主题（原型阶段先验证布局，不验证主题）

---

## 未在原型中实现的内容

以下功能在用户认可方向后再规划：

| 功能 | 原因未实现 | 需要确认的问题 |
|------|-----------|-------------|
| 跨工作区拖拽 | 交互复杂，先验证概念 | 用户需要从哪里拖到哪里？节点、消息、还是文件？ |
| Panel 宽度拖拽调整 | 增加原型复杂度 | 需要吗？还是固定宽度 + 折叠按钮就够？ |
| 对话继续（真实 AI 响应）| 无后端 | 是否需要在原型里 mock 一个 AI 回复动画？ |
| 节点图自动布局 | 布局算法复杂 | 当分支超过 10 个节点时，手动写死布局不现实 |
| Panel 2 分支树编辑 | 当前是只读 | 用户需要在树里直接操作（删除分支、重命名节点）吗？ |
| 多项目对话切换 | Panel 1 只展示了项目列表 | 切换项目后 Panel 2 的树要怎么变？ |

---

## 原型验证清单

用户评估 prototype.html 时，请关注：

- [ ] 四面板布局是否符合预期的信息密度？（太挤 / 刚好 / 太空旷）
- [ ] 点击分支树节点 → Panel 3 更新线性历史，这个交互逻辑是否直觉？
- [ ] 顶部工作区 Tab 的概念是否有用？还是一个 Tab 足够？
- [ ] Panel 4 的定位（只读详情）是否满足需求，还是需要变成可编辑区？
- [ ] 工具管理模式（Tab 2）是否是你想要的 SKILL.md 查看/管理方式？
- [ ] 控制台终端（Tab 3）的执行面板是否是你想要的服务管理方式？
- [ ] 整体视觉风格（浅色 Minimalist）是否合适？

---

## 下一步（用户认可原型后）

1. 将验证清单中的反馈转化为具体需求（req-001 起，放入 requirements/）
2. 选技术栈（React + Zustand？纯 HTML 渐进增强？）
3. 写 v0.1 产品规划文档（changelog/v0.1/product.md），覆盖对话模式核心
4. 开始技术规划（changelog/v0.1/technical.md）
